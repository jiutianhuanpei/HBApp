#include "zoomcontrol.h"
#include <

ZoomControl::ZoomControl(QWidget *parent) : QWidget(parent)
{

    closeBtn = new ColorButton(ColorButton::Close);
    minBtn = new ColorButton(ColorButton::Min);
    maxBtn = new ColorButton(ColorButton::Max);






}
